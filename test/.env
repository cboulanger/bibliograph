#
# Environment variables needed for running tests
#

YII_DEBUG=1
YII_ENV=test

# The PHP version to use for compiling & running the project
PHP_VERSION=7.3

# The url of the yii2 JSON-PRC endpoint from within the PHPFarm Docker instance
JSONRPC_ENDPOINT=http://host.docker.internal:8073/test/codeception/web

# The URL of the bibliograph web application from the docker host
BIB_SERVER_URL=http://localhost:8073

# Database settings
DB_TYPE=mysql
DB_HOST=host.docker.internal
DB_PORT=3306
DB_DATABASE=tests
DB_USER=bibliograph
DB_PASSWORD=bibliograph
DB_ROOT_PASSWORD=bibliograph
