# API tests wit a webserver
actor: ApiTester
bootstrap: _bootstrap.php
modules:
    enabled:
        - Asserts
        - REST:
            depends: PhpBrowser
        - PhpBrowser:
              url: "%JSONRPC_ENDPOINT%"
        - \Helper\Api
env:
    development:
    testing:
    setup:
    upgradev2:
    upgradev3:
